import {GraphQLExplorer} from '../../components/api-access'

/** Parity with api-access/rest, not actually used for the moment. Show that it can be used though */
export default function GraphQLExplorerPage() {
  return <GraphQLExplorer />
}
