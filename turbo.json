{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "download": {
      "outputs": ["*.oas.json"]
    },
    "generate": {
      "dependsOn": ["^generate", "download"],
      "cache": false,
      "outputs": ["*.oas.d.ts"]
    },
    "build": {
      "dependsOn": ["^build"]
    },
    "pub": {"cache": false, "dependsOn": ["build"]}
  }
}
